# GPU 사용 시 torch는 아래 링크에서 CUDA 버전에 맞게 먼저 설치: 
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# ── 시스템 의존성 (pip 아닌 apt로 설치) ──────────────────────
#   sudo apt install ffmpeg
#   (ffmpeg + ffprobe 모두 포함)

# ── Core & Utilities ──────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0
librosa
pymupdf
psycopg2-binary
python-pptx
ImageHash>=4.3.1
json-repair

# ── AI & API Clients ──────────────────────────────────────────
google-generativeai>=0.8.0
google-genai>=0.8.0
groq
openai>=1.0.0
anthropic>=0.49.0

# ── ML & Vision ───────────────────────────────────────────────
torch>=2.0.0
transformers>=4.40.0
ultralytics>=8.0.0

# ── Text & Knowledge Graph ────────────────────────────────────
kiwipiepy
pyvis>=0.3.2
graphrag==3.0.9

# ── Web API (질의 시스템) ─────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0

# ── Neo4j (Stage 6 이후 Parquet 적재) ─────────────────────────
neo4j>=5.14.0

# ── LanceDB / Parquet (Stage 7 + 질의 서비스) ─────────────────
lancedb>=0.14.0
pyarrow>=15.0.0
pandas>=2.0.0
