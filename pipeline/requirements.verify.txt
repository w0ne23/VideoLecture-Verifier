# VeriLec verify 파이프라인 전용 의존성 (preprocess + verifier 경로)
# Docker/배포는 이 파일만 설치하면 됨 (backend/Dockerfile에서 사용).
#
# requirements.txt는 원본 프로젝트에서 가져온 전체 목록으로, 그래프/검색/추천
# 잔재 의존성(graphrag, lancedb, neo4j, pandas 등)이 섞여 있어 설치 대상이 아님.
# [GRAPH-REMNANT] 정리(pipeline/main.py 참고)가 끝나면 requirements.txt는 삭제 예정.
#
# 시스템 의존성 (pip 아닌 apt로 설치):
#   sudo apt install ffmpeg   (ffmpeg + ffprobe 모두 포함)
#
# GPU 사용 시 torch는 CUDA 버전에 맞게 먼저 설치 (ultralytics가 torch를 의존):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# ── Core & Utilities ──────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0
librosa
ImageHash>=4.3.1
json-repair

# ── AI & API Clients ──────────────────────────────────────────
google-genai>=0.8.0
groq
openai>=1.0.0
anthropic>=0.49.0

# ── Vision (사람 마스킹: YOLO) ────────────────────────────────
ultralytics>=8.0.0

# ── Text ──────────────────────────────────────────────────────
kiwipiepy
