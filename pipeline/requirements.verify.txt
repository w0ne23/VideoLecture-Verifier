# VeriLec verify 파이프라인 의존성 (preprocess + verifier 경로)
# Docker/배포는 backend/requirements.txt와 이 파일을 설치하면 됨 (backend/Dockerfile 참고).
#
# 시스템 의존성 (pip 아닌 apt로 설치):
#   sudo apt install ffmpeg   (ffmpeg + ffprobe 모두 포함)
#
# GPU 사용 시 torch는 CUDA 버전에 맞게 먼저 설치 (ultralytics가 torch를 의존):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# ── Core & Utilities ──────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0
librosa
ImageHash>=4.3.1
json-repair

# ── AI & API Clients ──────────────────────────────────────────
google-genai>=0.8.0
groq
openai>=1.0.0
anthropic>=0.49.0

# ── Vision (사람 마스킹: YOLO) ────────────────────────────────
ultralytics>=8.4.90
# TensorRT is installed by backend/Dockerfile only for the NVIDIA build.
kornia>=0.7.0

# ── Text ──────────────────────────────────────────────────────
kiwipiepy
