# Verifier models are registered dynamically from the endpoint/model bindings
# selected in the web UI. No provider or model is preselected here.
model_list: []

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  database_url: os.environ/DATABASE_URL

litellm_settings:
  drop_params: true
