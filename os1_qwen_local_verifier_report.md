# os1-1 로컬 Qwen Verifier 벤치마크

## 실행 및 시간

- 입력/출력: `storage/inputs/os1-1.mp4` -> `storage/os1-qwen-local`
- Verifier LLM: 모든 단계 `ollama:qwen3.5:9b` (산출물 provider는 `ollama`만 존재)
- 전체 관측 시간: 약 13분 23초 (`02:42:31` ~ `02:55:54`)
- Verifier: 262.047초 (claim 115.875, issue detection 38.304, classifier 87.124, final 4.198, web 0.001, slide error 16.514초)

## Claim 품질

- 63개, 필수 필드 누락 0개. 유형은 definition 26, relationship 5, causal 31, currentness 1개.
- 정확 중복: `CL0004=CL0011`, `CL0006=CL0009`; 높은 유사 중복: `CL0005~CL0008` (0.881).
- 진행 문장 오검출: `CL0002`, `CL0003`, `CL0026`, `CL0030`; 불완전: `CL0045`; 유형 오분류 가능: `CL0052`.
- 위 결함 9건을 제외한 휴리스틱 유효 claim은 54/63 (85.7%). 정답 라벨 기반 정확도가 아닌 산출물 품질 점검치다.

## Claim 전체 목록

- `CL0001` (S2): 다중프로그래밍은 운영체제 발전 과정에서 나타났으며 컴퓨터 시스템에 지대한 영향을 끼친다.
- `CL0002` (S2): 운영체제의 종류와 특징을 간단히 이해하는 것이 이 장의 목적이다.
- `CL0003` (S3): 운영체제의 정의를 설명한다.
- `CL0004` (S4): 운영체제는 실체가 있는 소프트웨어이다.
- `CL0005` (S4): 운영체제는 사용자와 하드웨어 사이의 중개자로 프로그램 실행을 관리·제어하는 시스템 소프트웨어이다.
- `CL0006` (S4): 운영체제는 컴퓨터가 켜질 때 처음 적재되어 모든 프로그램 실행을 제어하고 사용자 요청을 처리한다.
- `CL0007` (S4): 운영체제는 컴퓨터 자원을 독점적으로 관리한다.
- `CL0008` (S4): 운영체제는 사용자와 하드웨어 사이에서 중계하며 프로그램 실행을 관리·제어한다.
- `CL0009` (S4): 운영체제는 컴퓨터가 켜질 때 처음 적재되어 모든 프로그램 실행을 제어하고 사용자 요청을 처리한다.
- `CL0010` (S4): 운영체제는 컴퓨터가 아니다.
- `CL0011` (S4): 운영체제는 실체가 있는 소프트웨어이다.
- `CL0012` (S5): 운영체제는 하드웨어를 독점적으로 사용한다.
- `CL0013` (S5): 응용 프로그램이 하드웨어에 접근하려면 반드시 운영체제에 요청해야 한다.
- `CL0014` (S5): 자원에 대한 모든 접근 권한은 운영체제에게만 있다.
- `CL0015` (S5): 키보드 값을 읽을 때 응용 프로그램은 운영체제에 요청한다.
- `CL0016` (S5): 모니터 출력이나 키보드 값 전달은 운영체제가 대신 수행한다.
- `CL0017` (S6): 운영체제는 모든 컴퓨터 자원을 관리한다.
- `CL0018` (S6): 하드웨어 자원은 CPU, 캐시, 메모리, 키보드 등을 포함한다.
- `CL0019` (S6): 소프트웨어 자원은 응용 프로그램이다.
- `CL0020` (S6): 응용 프로그램 생성·삭제·실행·다운로드는 사용자 요청에 따라 운영체제가 처리한다.
- `CL0021` (S6): 데이터 자원은 파일이다.
- `CL0022` (S6): 파일 생성·삭제 권한은 운영체제가 가지며 사용자는 운영체제에 요청해야 한다.
- `CL0023` (S6): 운영체제는 자원 할당·공유·접근, 디스크 빈 공간과 파일 위치를 독점 관리해 프로그램 간 충돌을 방지한다.
- `CL0024` (S6): 운영체제는 실행 프로그램, 메모리, 입출력 권한, 사용자 계정과 프로그램의 전체 생명주기를 관리한다.
- `CL0025` (S6): 운영체제는 소프트웨어이며 광의로 커널·도구·드라이버, 협의로 커널을 의미한다.
- `CL0026` (S6): 운영체제 정의를 본 후 목적과 기능을 설명한다.
- `CL0027` (S7): 운영체제의 목적은 사용자 편의 제공이다.
- `CL0028` (S7): 운영체제의 목적은 편의 제공과 자원 관리 효율성 제공이다.
- `CL0029` (S7): 운영체제의 목적은 편리성과 자원 관리 효율성 두 가지다.
- `CL0030` (S7): 운영체제 기능과 역할은 현재 이해하기 어렵지만 키워드를 기억해야 한다.
- `CL0031` (S8): 운영체제는 하드웨어를 직접 제어하고 입출력을 수행한다.
- `CL0032` (S8): 사용자와 응용 프로그램은 운영체제의 기능 범위 안에 있다.
- `CL0033` (S8): 저장된 프로그램 중 현재 실행 중인 것을 프로세스라고 한다.
- `CL0034` (S8): 동시에 실행되는 각 프로그램의 실행 단위를 프로세스라고 한다.
- `CL0035` (S8): 운영체제는 요청된 프로그램을 디스크에서 메모리로 적재해 실행한다.
- `CL0036` (S8): 사용자 종료 요청 시 운영체제가 프로세스를 종료한다.
- `CL0037` (S8): 운영체제는 어떤 프로세스를 실행할지 스케줄링한다.
- `CL0038` (S8): 자원 충돌 시 운영체제가 프로세스 동기화를 수행한다.
- `CL0039` (S8): 프로세스 적재 시 운영체제가 실행 메모리를 할당한다.
- `CL0040` (S8): 프로세스 종료 시 운영체제가 할당 메모리를 회수한다.
- `CL0041` (S8): 메모리 부족 시 프로그램은 `malloc`, `new` 등으로 운영체제에 할당을 요청한다.
- `CL0042` (S8): 프로세스 간 메모리 침범을 막는 메모리 보호는 운영체제 기능이다.
- `CL0043` (S8): 디스크를 메모리 영역으로 확장하는 기법은 가상 메모리이며 메모리 관리 기능이다.
- `CL0044` (S8): 파일 생성·삭제·열기·닫기·읽기·쓰기·위치·속성 관리는 파일 시스템 관리다.
- `CL0045` (S8): 장치는 직접 관리하는 것.
- `CL0046` (S8): 키보드, 마우스, 디스플레이, 디스크 작업은 장치 관리다.
- `CL0047` (S8): 운영체제는 네트워크 입출력과 연결 상태를 관리한다.
- `CL0048` (S8): 운영체제는 외부 침입 유무를 관리한다.
- `CL0049` (S8): 운영체제는 사용자 계정을 관리한다.
- `CL0050` (S8): 운영체제는 CPU·메모리 활용률 통계를 낸다.
- `CL0051` (S8): 오류 발생 시 시스템 종료 또는 부팅 대응은 운영체제 기능이다.
- `CL0052` (S8): 시스템 종료와 부팅은 운영체제의 보편적 공통 기능이다.
- `CL0053` (S9): 운영체제의 목적은 자원 관리다.
- `CL0054` (S9): 운영체제는 프로그램에 공통 서비스를 제공한다.
- `CL0055` (S10): 운영체제 목적은 하드웨어·소프트웨어 자원 관리다.
- `CL0056` (S10): 운영체제 목적은 자원의 효율적 사용이다.
- `CL0057` (S10): 운영체제 목적은 사용자 편의 제공이다.
- `CL0058` (S10): 응용 소프트웨어는 사용자 요구 충족을 위해 설계된다.
- `CL0059` (S10): 응용 소프트웨어는 특정 사용자의 특정 욕구 충족을 위해 설계된다.
- `CL0060` (S10): 응용 소프트웨어는 만든 특정 목적만 수행한다.
- `CL0061` (S10): 응용 소프트웨어 개발 언어는 C, C++, 어셈블리어다.
- `CL0062` (S10): 운영체제는 부팅부터 종료까지 상주하며 응용 프로그램 요청을 처리한다.
- `CL0063` (S10): 응용 소프트웨어는 사용자 명령으로 실행·종료되고 자원 사용은 운영체제에 요청하며, 운영체제는 자원을 배타적으로 사용한다.

## Issue Detection

63개 중 19개 후보를 검출했다. 순서는 `issue/claim, confidence, 요약`이다.

- `I0001/CL0013` 0.85, 하드웨어 접근은 반드시 OS를 경유
- `I0002/CL0014` 0.85, 모든 자원 접근 권한은 OS에만 존재
- `I0003/CL0015` 0.85, 키보드 값은 OS에 요청
- `I0004/CL0016` 0.85, 출력·입력 전달을 OS가 대행
- `I0005/CL0017` 0.85, OS가 모든 컴퓨터 자원을 관리
- `I0006/CL0018` 0.85, 하드웨어 자원 예시
- `I0007/CL0031` 0.95, OS의 하드웨어 직접 제어
- `I0008/CL0032` 0.85, 사용자·앱이 OS 기능 범위 내 존재
- `I0009/CL0033` 0.90, 프로세스 정의
- `I0010/CL0034` 0.88, 실행 단위로서 프로세스
- `I0011/CL0035` 0.82, 디스크에서 메모리로 적재
- `I0012/CL0037` 0.80, 프로세스 스케줄링
- `I0013/CL0038` 0.85, 프로세스 동기화
- `I0014/CL0055` 0.95, OS 목적은 자원 관리
- `I0015/CL0056` 0.90, OS 목적은 효율성
- `I0016/CL0057` 0.85, OS 목적은 편의성
- `I0017/CL0058` 0.80, 응용 SW 정의
- `I0018/CL0059` 0.85, 응용 SW의 특정 욕구 충족
- `I0019/CL0060` 0.82, 응용 SW가 특정 목적만 수행

정상 설명도 대거 후보화되어 detector 정밀도는 낮았다. 후속 의미 판단상 19개 중 17개는 오류가 아니었다.

## Classifier

- `scope_overclaim` 2건: `I0002`, `I0019`.
- 나머지 17건은 모델이 “오류가 아님”이라고 판단하며 확률을 모두 0으로 반환했다. 프롬프트 본문의 합계 1.0 지시와 달리 JSON 예시가 네 확률 모두 0.0으로 작성돼 있던 것이 직접 원인이었다.
- 이 17건은 `parse_failed` 및 `final_issue_type=None`이 되었고 다음 단계 입력에서 빠졌다. 이는 classifier의 정상적인 기각이 아니라 파싱 실패가 후보 제거처럼 작동한 버그다.
- `I0002`: BIOS/UEFI·펌웨어 등 OS 외 주체까지 배제하는 과도한 일반화로 분류.
- `I0019`: “오직 특정 목적만”이 다른 활용 가능성을 배제하므로 과도한 일반화로 분류.

## Final Verifier

- 입력 2, 확정 0, 검토 필요 0, 기각 2.
- `I0002`: 정상 파싱. 교육 문맥의 핵심 개념 강조로 보아 오류가 아니라고 기각.
- `I0019`: JSON 구문 오류로 파싱 실패 후 fallback 기각. 이 결과는 신뢰하기 어려워 재검증 대상이다.
- 슬라이드 오타 0건.

## Web Grounding

- 활성화됐지만 기본 대상인 `factual_error`, `temporal_error`가 0건이었다.
- 검색/Qwen grounding 호출 0건, `grounded_issue_count=0`, `status_counts={}`.
- 기능 실패가 아니라 적용 대상 부재다. 이 실행으로 검색·grounding 품질은 평가할 수 없다.

## 결론

- Claim 내용 보존은 대체로 양호하지만 중복, 진행 멘트, 불완전 claim 제거가 필요하다.
- Issue detector 과검출이 크다.
- Classifier에 `not_issue`를 공식 지원하고 all-zero 응답을 파싱 실패로 취급하지 않아야 한다.
- Final verifier JSON 출력 복구가 필요하다.
- 실제 factual/temporal 오류가 있는 샘플로 web grounding을 별도 측정해야 한다.
